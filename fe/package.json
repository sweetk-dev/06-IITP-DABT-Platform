{
  "name": "iitp-dabt-platform-fe",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.27.0",
    "@iitp-dabt-platform/common": "file:../packages/common"
  },
  "peerDependencies": {
    "typescript": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "@types/react": "^18.2.15",
    "@types/react-dom": "^18.2.7",
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.4.0"
  },
  "scripts": {
    "dev": "vite",
    "dev:clean": "npm run clean:cache && vite",
    "build": "npm run clean && npm run clean:cache && tsc -b && vite build",
    "build:clean": "npm run clean && npm run clean:cache && tsc -b && vite build",
    "preview": "vite preview",
    "clean": "rimraf dist",
    "clean:cache": "rimraf node_modules/.vite"
  }
}

