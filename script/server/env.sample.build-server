# ===========================================
# 빌드 서버용 환경 변수 설정 파일
# ===========================================
# 사용법: cp env.sample.build-server .env
# 목적: build-server*.js 스크립트 실행에 필요한 환경 변수만 포함

# ===========================================
# Git 설정
# ===========================================
# Git 저장소 URL (HTTPS 또는 SSH)
GIT_REPO_URL=https://github.com/sweetk-dev/06-IITP-DABT-Platform.git

# Git 브랜치 (기본값: main)
GIT_BRANCH=main

# ===========================================
# 경로 설정
# ===========================================
# 소스 코드 경로 (기본값: /home/iitp-plf/iitp-dabt-platform/source)
SOURCE_PATH=/home/iitp-plf/iitp-dabt-platform/source

# 배포 폴더 경로 (기본값: /home/iitp-plf/iitp-dabt-platform/deploy)
DEPLOY_PATH=/home/iitp-plf/iitp-dabt-platform/deploy

# (선택) 실행 서버의 운영 스크립트 경로 (deploy-server-ops.js에서 사용)
OPS_SCRIPT_PATH=/var/www/iitp-dabt-platform/script

# ===========================================
# 빌드 설정
# ===========================================
# Node.js 환경 (production, stage, development)
NODE_ENV=production

# npm production 모드 (true/false)
NPM_CONFIG_PRODUCTION=true

# ===========================================
# 배포 설정 (deploy-server.js에서 사용)
# ===========================================
# 실행 서버 설정
PROD_SERVER_HOST=your-prod-server.com
PROD_SERVER_USER=iitp-plf
PROD_SERVER_PORT=22
PROD_BE_PATH=/var/www/iitp-dabt-platform/be
PROD_FE_PATH=/var/www/iitp-dabt-platform/fe

# ===========================================
# 보안 설정
# ===========================================
# 빌드 시 사용할 SSH 키 경로 (선택사항)
SSH_KEY_PATH=

# Git 인증 토큰 (선택사항)
GIT_TOKEN=

